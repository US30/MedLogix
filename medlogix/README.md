# MedLogix Project Module

This folder contains the full MedLogix code, datasets, vector database, tests, and trained artifacts.

For complete repository documentation (including `Outputs/`), see the root README:

- `../README.md`
